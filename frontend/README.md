# X5-Enot

Frontend MVP for candidate quiz.

## Run

```bash
npm install
npm run dev
```

Default backend URL:

```txt
http://localhost:8080/api/v1
```

To override it, create `.env`:

```txt
VITE_API_BASE_URL=http://localhost:8080/api/v1
```
